<%= form_with(model: pizza_topping) do |form| %>
  <% if pizza_topping.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pizza_topping.errors.count, "error") %> prohibited this pizza topping from being saved:</h2>

      <ul>
        <% pizza_topping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pizza_id, style: "display: block" %>
    <%= form.number_field :pizza_id %>
  </div>

  <div>
    <%= form.label :topping_id, style: "display: block" %>
    <%= form.number_field :topping_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
