<%= form_with(model: pizza) do |form| %>
  <div class="row align-items-center">
    <div class="col-md-1">
      <h4><%= form.label :name, "Name:", { class: "form-label form-label-lg"} %></h4>
    </div>
    <div class="col-md-8">
      <%= form.text_field :name, { class: "form-control form-control-lg form-control-custom",  maxlength: 50, } %>
    </div>
    <div class="col-md-3">
      <% if new_pizza %>
        <%= form.submit "Create Pizza", class: "btn btn-block btn-primary" %>
      <% else %>
        <%= form.submit "Update Pizza Name", class: "btn btn-block btn-primary" %>
      <% end %>
    </div>
  </div>
<% end %>
